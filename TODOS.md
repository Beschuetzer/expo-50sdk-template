# TODOS
- I want you to add docs for large language models so they how this repo works and how to work within it.  FOr example, that the mobile app has internationalization and no text should be hard-coded as well as dark and light modes so styling should be supportive of that.
- Please add tests for all the relevant functionality in this template repo (if it doesn't already exist)?
- I want you to create a theme-aware reusable component called "ScreenWithButton".  THis screen be used in the home page screen and it will have a button positioned at the bottom and a scrollview/flatlist/flashlist as the main content part.  It should have default padding and stlying for the theme stuff.  The idea is to make it as easy as possible to create a new screen with everything already baked in.
- Set up a pattern for sharing types between both the api and mobile apps (a shared types project or something else?)
- clean up remaining stuff that isn't really necessary