# API

Minimal Node.js backend for local development.

- `GET /health` returns a JSON health response.
- Default host: `0.0.0.0`
- Default port: `4200`

Run it through Nx with `npm run api`, or start the Expo app and API together with `npm run dev`.
